{
  "name": "echorag",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "uvicorn backend.api.server:app --reload --port 8000",
    "test": "python -m unittest discover -s tests -v",
    "bench": "python -m benchmarks.harness --queries 300"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.6"
  },
  "description": "Voice-enabled RAG over ai4bharat/MSMARCO-XI"
}